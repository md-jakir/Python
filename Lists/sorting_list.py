# sorting the number of list or string list using sort() function

numbers = [10, 2, 0, -1, -2, 6, 8, 3]
numbers.sort()
numbers
[-2, -1, 0, 2, 3, 6, 8, 10]

# Mutable and Immutable
# in a List can modify the index's value this is mutable and in a string can't change value is called immutable
# tuple is immutable and written with () instead of []. 
          
